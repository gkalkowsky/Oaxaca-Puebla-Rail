# Core analysis
openpyxl>=3.1
pandas>=2.0

# PDF extraction. marker-pdf is the preferred extractor; it is a large install
# and pulls model weights (both gitignored). extract_source.py falls back to
# pdftotext (poppler-utils) or pypdf when marker is unavailable, and records
# which extractor produced the output.
pypdf>=4.0
# marker-pdf>=1.0        # uncomment when running full-fidelity extraction
